Implement `TASK.md` completely in this repository.

Work autonomously and do not ask questions. Use only dependency-free HTML, CSS,
and JavaScript; do not install packages, use external assets, call external
services, or require a build step. Do not commit. Do not start a persistent
server.

Choose the interface structure yourself. Implement every required state and
behaviour in the task. Before handoff, exercise the core flow at wide and narrow
viewport assumptions and create `HANDOFF.md` describing what you implemented,
the exact checks you actually performed, and every failed, missing, blocked, or
unreachable check. Do not report an unexercised state as passing.
